[TASK INSTRUCTION FOR CLAUDE]
Target: Split Test/Stable Environments and Implement Promotion Workflow
Priority: High
Issue: #27

## Context
- `deploy.js` currently restarts both containers and copies DB to both.
- We need to isolate development to the TEST environment.

## Specific Tasks
1. **Modify `deploy.js`**:
   - Add argument parsing: `const target = process.argv[2] === '--stable' ? 'stable' : 'test';`
   - If `target === 'test'`:
     - Run `docker-compose up -d --build cmms-test`.
     - Only copy `db.json` to `cmms-test`.
     - Only restart `cmms-test`.
   - If `target === 'stable'`:
     - Run `docker-compose up -d --build cmms-stable`.
     - Only copy `db.json` to `cmms-stable`.
     - Only restart `cmms-stable`.
2. **Update all task files in `brain/tasks_queue/`**:
   - Loop through `issue_23_bms_integration.txt`, `issue_24_ui_polish.txt`, `issue_25_copilot_integration.txt`, `issue_26_system_audit.txt`.
   - Append to the end of each: `IMPORTANT: Deployment must be TEST ONLY. Run: node deploy.js`.
3. **Update WORKFLOW.md**:
   - Add "Phê duyệt & Phát hành (Staging & Release)" section.
   - Specify URLs:
     - Test: http://vanhanh.synology.me:8081
     - Stable: http://vanhanh.synology.me:8080 (or main URL)
4. **Finish**:
   - Commit, Push, Close Issue #27.
