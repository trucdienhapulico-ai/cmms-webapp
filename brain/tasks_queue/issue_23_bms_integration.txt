[TASK INSTRUCTION FOR CLAUDE]
Target: Implement BMS Webhooks & IoT Integration API
Priority: Medium
Issue: #23

## Context
- Backend file: server.js
- Access Control: Use a simple X-API-Key header for external requests.

## Specific Tasks
1. **Backend - Schema**:
   - Add `apiKeys: []` and `webhooks: []` to DB.
2. **Backend - Middleware**:
   - Create `validateApiKey` function to check header `x-api-key`.
3. **Backend - External Alert API**:
   - `POST /api/external/alert`: (Authenticated by API Key).
   - Payload: `{ assetId, faultCode, description, severity }`.
   - Action: Automatically create a Work Order with priority based on severity.
4. **Frontend - UI**:
   - Add "Ket noi" section in sidebar (Admin only).
   - `renderIntegration()`: Manage API Keys (Generate/Revoke) and Webhooks (URL, Events).
5. **UI Map**:
   - Add [UI-Int-ApiKeys] and [UI-Int-Webhooks] to `renderUIMap()`.
6. **Roadmap**:
   - Check off "System Integration" in `brain/roadmap.md`.
7. **Finish**:
   - Commit, Push, Close Issue #23.
