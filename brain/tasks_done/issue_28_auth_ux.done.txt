[TASK INSTRUCTION FOR CLAUDE]
Target: Auth UX Improvements & Admin Password Reset
Priority: Medium
Issue: #28

## Context
- Backend: server.js
- Frontend: public/index.html
- Default Password: Use the one provided by Admin (DNHc4&982!cjhDB) - DO NOT PRINT THIS PLAIN TEXT IN COMMIT MESSAGES.

## Specific Tasks

### 1. Login UX
- In `public/index.html`, add a "Ghi nhớ mật khẩu" checkbox to the login form.
- Add an error message div: `<div id="login-error" style="color:red; margin-top:10px; display:none"></div>`.
- In `login()` function:
  - If successful, and "Remember Me" is checked, save credentials to `localStorage`.
  - If failed, show the error message in `login-error`.
- On page load, auto-fill credentials if they exist in `localStorage`.

### 2. Password Reset (Backend)
- Add `POST /api/users/:id/reset-password`.
- Only Admin can call this.
- Reset the user's `passwordHash` using the system default password.
- Log to Audit: `Admin reset password for user: [username]`.

### 3. Verification
- Test login with "Remember Me".
- Test Admin resetting a user password.

### 4. Finish
- Deployment: node deploy.js (Test environment only).
- Commit, Push, Close Issue #28.


--- MERGED FROM tasks_queue\issues.txt ---

DANH SÁCH ISSUE CẦN XỬ LÝ
===========================

Issue #1: Sửa README.md
-----------------------
Mô tả: Cập nhật địa chỉ truy cập cũ thành http://localhost:3090
File cần sửa: README.md
Logic cần thực hiện: Tìm tất cả các URL/địa chỉ cũ trong README.md và thay thế bằng http://localhost:3090

Issue #2: Thêm tính năng đổi mật khẩu
---------------------------------------
Mô tả: Cho phép người dùng tự đổi mật khẩu của mình
File cần sửa:
  - server.js (thêm API endpoint PUT /api/users/me/password)
  - public/js/ (thêm UI form đổi mật khẩu)
Logic cần thực hiện:
  - Backend: Xác thực mật khẩu cũ, hash mật khẩu mới bằng PBKDF2+SHA256, lưu vào db.json
  - Frontend: Form gồm 3 trường (mật khẩu cũ, mật khẩu mới, xác nhận mật khẩu mới)
  - Validation: Mật khẩu mới tối thiểu 5 ký tự, 2 trường mới phải khớp nhau
